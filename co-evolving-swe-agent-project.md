# Co-Evolving 双角色 SWE Agent —— 项目规划文档

> 本文档面向接手实验的 Claude Code agent。它不只列结论，也给出每个决策**为什么**这么定，
> 以便在落地中遇到冲突时能回到原始理由判断，而不是机械执行。
> 阅读顺序：先读「核心 idea」与「术语锚点」，再读「已锁定的设计决策」，最后按「待完成事项」从第一项开始执行。

---

## 1. 核心 idea 与意义

### 1.1 一句话主张

我们提出一个 **co-evolving 双角色 SWE agent 范式**：不引入更大的 teacher 模型，而是用
**廉价且精确的符号信号（代码静态分析）** 替代昂贵的大模型监督，为一个负责改代码的 code agent
构造更好的训练轨迹；同时把「该跑什么分析、怎么把结果呈现给 code agent」本身也变成一个可学习的
analysis agent，两个角色协同进化。

### 1.2 三层主张（这是论文的叙事主轴，也决定 baseline 怎么设）

整套工作是**一条线、三层**，不是三个并列的创新点（并列会被读成「每个都不够深」）：

1. **Setting 层（系统主张）**：co-evolving 双角色范式打得过现有 SWE agent。
2. **Contribution 层（算法主张）**：该范式的核心算法难点落在
   **双边多轮信用分配（bilateral multi-turn credit assignment）** 上——这是 agentic RL
   一个未被正面处理的开放场景。我们给出解法 X。
3. **Insight 层（分析主张）**：通过「定位难度 × 修复难度」二维框架，揭示增益来自哪里、
   是否两个角色都必须拉满、co-evolution 的前沿如何推进。

> 叙事收口：「我们提出 co-evolving 双角色 SWE 范式（外层 baseline 证明它打得过现有 agent），
> 其核心算法难点是双边多轮信用分配（内层 baseline 证明朴素 credit 不够、X 才行），
> 而这个难点本身就是 agentic RL 一个未被正面处理的开放场景（范式即贡献，不是借来的实验场）。」

### 1.3 为什么这个 idea 有意义 / 新意在哪

- **不靠更大的 teacher**：现有 SWE agent 范式（大小模型协同、或直接上商业大模型）总需要更强的模型参与。
  我们用静态分析这种 compute-efficient 的精确符号信号取代它。
- **被调用的工具是内生、在学的**：当前 agentic RL for coding 里，被调的工具（LSP、test runner）是
  **外生**的、行为不随训练改变。本工作里 analysis agent 这个「工具」本身在学习，于是问题从
  「多轮信用分配」升级为「**双边**多轮信用分配」——一次成功要同时摊给 code agent 的某几轮动作、
  和 analysis agent 的某几轮供给，且两条 credit 流相互耦合（这轮分析有没有用，取决于 code agent
  下一轮会不会用对）。据现有了解，这个具体形态尚无人正面处理。

### 1.4 一个被推翻、需避免的措辞

**不要再用「特权信息 / privileged information (LUPI)」这个词。** LUPI 严格意义是「训练可见、测试不可见」。
我们已确定静态分析工具**训练与测试时都在**（贴近真实部署：现实 SWE agent 本就带 LSP/tree-sitter/CodeQL）。
工具测试时也在，它就不是特权信息，而是普通工具增强。
**内核未塌**——「用便宜精确的符号信号替代昂贵 teacher」与工具在不在测试时无关，依然成立；
我们只是放弃了 LUPI 那层论文意义上的新意，换来不必做「把符号信号蒸馏进权重」这件又难又易掉性能的事。
这个交易划算。但措辞上务必避开「特权信息」，否则一定被审稿人挑。

---

## 2. 术语与机制锚点（先对齐定义，后面反复用到）

- **analysis agent（分析角色）**：输入失败信号（repo + failing test + traceback），
  在**现成静态分析原语之上**（tree-sitter / joern / LSP 这一层，**固定、不学**），
  做 query + 切片 + 压缩，产出**面向此 bug 的定位摘要**。
  - 关键：它学的**不是「写一个分析器」**（AST / call graph / data flow 的*抽取*基本是已解决问题，
    重写 tree-sitter 没意义）。它学的是**「选择与呈现」**：给定失败信号，该追哪几个函数的调用图邻域、
    trace 哪些变量的数据流、怎么把一张几百节点的图压成真正相关的 ~8 个节点、用什么形式塞进 code agent
    有限的上下文并指向修复点。**动作空间 = 在静态分析原语之上做 query + 切片 + 压缩 + 呈现的策略。**
- **code agent（修改角色）**：拿定位摘要去改代码，产出 patch / diff。
- **ground truth**：跑测试，过 / 不过。这是**唯一的 terminal 锚点**。
- **dump baseline**：无脑全量 AST / call graph（不学也能有的笨办法）。后面反事实奖励、冷启动都以它为对照。
- **有用带（useful band）**：相对当前 code agent 能力而言，「好分析能把任务从 fail 顶成 pass」的那撮任务。
  只有这撮任务在教 analysis agent（见 §5）。

---

## 3. 已锁定的设计决策（含理由）

### 3.1 工具时态：训练 + 测试都在 ✅
理由见 §1.4。直接后果：放弃「特权信息」措辞。

### 3.2 analysis agent 测试时在线（不冻结成固定工具库）✅
一套冻结的通用工具最终会退化成「无脑 dump 整张 AST」，而那正是不学也有的东西。
值钱的是「针对这个 bug 该抽哪一小块」的**适应性**——冻结掉就没了。所以 analysis agent
测试时也在线，对每个具体 bug 现配分析。

### 3.3 多轮协作（带回合），非单轮顺序 ✅
- 真实开发就是多轮：看一眼、改一下、改不动回头问「X 的调用方有哪些」。
- 多轮的硬理由：analysis agent 第二轮可**条件于 code agent 的部分进展**（「试了改 X 没用，报错变成 Y」），
  这种信息对下一份分析极有价值，单轮拿不到。
- **代价**：干净的单对象反事实被打碎，退回经典的 **episode 内信用分配**——这正是 §1.2 的 Contribution 层。
- **副作用（正面）**：之前被判为「可有可无」的「线索清晰度」主观信号，在多轮下**真正有了用武之地**——
  terminal credit 被摊薄到多轮，逐轮 dense 信号成了降方差刚需。但定位不变：它只能是 **process / shaping reward**，
  **terminal 永远只认测试通过**。
- **新失败模式（必须防）**：两角色空转互捧（分析一直给、code 一直问、不收敛）。
  → 必须有**回合预算**，且 terminal **不奖励拖延**，否则它会学会用「看起来在协作」刷过程奖励。

### 3.4 双 backbone、异尺寸 ✅
- **同尺寸 → 可单 backbone；异尺寸 → 必须双 backbone**，这是物理事实。我们选异尺寸（分析侧较小、code 侧较大），
  于是双 backbone 被锁死。
- **method 段的理由要用「任务负载不对称 → 异尺寸 → 双 backbone」，不要用「容量竞争」**。
  原因：在 7B 这个档，真正咬人的常常是**模型绝对能力地板**（一个 7B 够不够强到产出能自举的轨迹），
  而非容量竞争；用「容量竞争」说理由会被审稿人追问「你怎么知道是竞争而不是模型太弱」。
- **双 backbone 抽走的旧设定（务必不要再用）**：
  - ❌ 「单策略双角色 / prompt 区分 / self-play 一致信号」——整条作废，它建立在单 backbone 上。
  - ❌ 角色串味 / output head / 角色 token 那套讨论——两网络物理隔离，串味不可能发生，无需处理。
  - ⚠️ **非平稳问题从「被缓解」退回「全额存在」**：两个独立网络各追一个在变的伙伴，GAN 式非平稳全额回来。
    → 「交替冻结」从 nice-to-have 升级为**刚需**，且现在是字面冻结一个网络、训另一个（语义反而更干净）。
  - ⚠️ **正迁移丢失**：两网络各自从头重学代码理解，小分析模型尤其吃亏（容量小又蹭不到大模型表征）。这是异尺寸的真实代价。

> **⚠️ 一个尚未验证、不能默认的假设**：「分析活比修复活轻 → 分析侧用小模型」。
> 「在几百节点的图里判断哪 8 个相关」是需要相当代码推理能力的任务，**某种程度上不比改代码轻**。
> 「分析活重」支持「双 backbone」（不能单 backbone 硬挤两个重任务），但它**反对**「分析侧更小」
> （若分析真重，该配对称甚至更大的模型）。**这两件事上一轮是捆着选的，现在必须拆开**：
> 双 backbone 站得稳，但「分析侧更小」这个尺寸假设正是 §6 待办第一项要去证伪/证实的东西。

### 3.5 尺寸配比：主实验 7B/7B，scaling 验证 7B/30B ✅（待 §6.1 闸门确认）
- 分析侧固定 7B，唯一变量是 code 侧 7B vs 30B。
- **起步用 7B/7B 做主实验**，理由**不是省钱**：code 侧 30B 太强，会把大量任务推进「不看分析也能解」的左下象限，
  导致 analysis agent 在冷启动和早期 co-evolve **收不到梯度（反事实边际≈0）**——
  也就是 30B 会让你**更难观测到分析到底有没有用**，而那正是全文要证的。7B/7B 把两边能力拉平，**有用带最宽**，信号最容易冒出来。
- **7B/30B 留作 scaling 章节**：在「已知范式有效」前提下，测它扛不扛得住强 code 模型（呼应 §5 有用带漂移）。
- 关于「7B/7B」要讲的故事是「**小系统不靠大模型也能打**」（呼应 §1.3 动机）；30B 一进来这个故事会被稀释，故 30B 仅作 scaling。

### 3.6 信用分配 baseline 选 dump（非空分析、非组内均值）✅
反事实边际定义为 `P(过 | 学到的分析) − P(过 | dump baseline)`。
选 dump 当 baseline，是因为它精确量出「学到的切片相对不学的笨办法多贡献了多少」，
直接对应卖点（学习的价值在选择与呈现、不在抽取）。
用空分析当 baseline 会把「静态分析本身有用」和「切片策略有用」两件事混回去。

### 3.7 奖励结构 ✅
- **唯一 terminal 锚点 = 测试通过**。绝不拿「线索清晰/模糊」的主观感受当 terminal——会被 game
  （analysis agent 会学会产出「看起来清晰、其实没用」的分析）。
- **code agent 信号**：过/不过直接做信号，STaR 式留成功轨迹做 SFT，或上 RL。
- **analysis agent 信号**：反事实边际（§3.6）。实现上不必每条真跑两遍——
  固定当前 code agent，对同一 bug 采样若干份不同分析摘要，用**组内相对优势（GRPO 味）**估计。
- 「线索清晰度」主观信号：仅作多轮下的 **shaping / 降方差** 项（§3.3），不作 terminal。

### 3.8 外循环：交替冻结（避免 GAN 式非平稳）✅
固定一边、训另一边，再交换。双 backbone 下这是字面冻结一个网络。
**同时也是干净的信用分配**——冻住一边，变化才能归因到另一边。

### 3.9 冷启动策略（方向已定，细节见 §6.2）✅
- 闭环第一圈的死结：analysis agent 初始随机 → 摘要是垃圾 → code agent 几乎全 fail →
  两条通道都收不到正信号 → 系统饿死在原点。
- 破局：**第一圈不让 analysis agent 自由发挥**。先用**固定的 dump 当冷启动的 baseline 分析**，
  把 code agent 训到一个非平凡水平（让它「有分析」时确实比「没分析」强）；这一步同时给了 analysis agent
  通道一个非零的反事实可学。闭环有了正梯度后，再松开 analysis agent 去优化切片。
- 双 backbone 把冷启动劈成**两个难度悬殊的子问题**：大 code 模型相对好暖（SWE 的 SFT / rejection sampling 现成）；
  **小 analysis 模型是硬骨头**——「好的切片」没有 ground truth，不能像 code 那样捞 gold patch 当标签
  （分析的价值只能由 code 的成功反推）。
- 顺序：**先暖大 code 模型 → 用一个已能干活的固定 code 模型当评判者，用反事实信号把小 analysis 模型从随机暖到非平凡 → 再合上 co-evolve 闭环。**

---

## 4. 实验设计：消融脊（baseline 的完整布局）

不要在「跟其他 SWE agent 比」与「跟其他 RL 算法比」之间二选一——那是伪困境，会两头不讨好。
正确做法是**两层 baseline + 一条贯穿的消融脊**：相邻两行只动一个变量，差值即该变量的净价值。

| # | 配置 | 相对上一行新增的唯一变量 | 分析角色状态 | 信用分配 | 守护层 | 隔离出的归因 |
|---|------|----------------------|------------|---------|-------|-----------|
| 0 | 现有单 agent SWE 方法（已发表，引用数字即可） | — | 无 | 各自原法 | 外层·横向参照 | 地板线 |
| 1 | 我方·单 agent | 去掉分析角色，其余同我方栈 | 无 | terminal | 外层 | 同栈下「没有分析角色」的对照 |
| 2 | 双角色·分析冻结 | 分析角色**存在但不学**（dump/固定启发式） | 冻结 | terminal 拍平 | 外层 | 行2−行1 = 「多一个角色 + 静态分析」值多少 |
| 3 | 双角色·co-evolve·朴素 credit | 分析角色**开始学**（co-evolve 打开） | 学习 | 轨迹级拍平给两角色 | 外层↔内层**枢纽** | 行3−行2 = 「co-evolution」值多少 |
| 4 | 双角色·co-evolve·单边 credit | **只给 code agent 正经多轮 credit**，分析仍拍平 | 学习 | code 正经 / 分析拍平 | 内层 | 行4−行3 = 「把 code 信用分配做对」值多少 |
| 5 | 双角色·co-evolve·**X**（本方法） | **分析角色也拿正经多轮 credit**（双边） | 学习 | 双边多轮 credit | 内层 | 行5−行4 = 「双边」那个「双」字值多少 = X 的内核 |

**用表的关键点：**
- **行 3 是枢纽**，必跑不可替代：对外层是「co-evolve 但朴素 credit」，对内层是「X 的朴素对照」，一份实验服务两层。
- **总增益三笔拆账**：行2−行1（双角色结构）、行3−行2（co-evolution）、行5−行3（信用分配做对）；
  第三笔再劈成行4−行3 与 **行5−行4（双边特异性）**。**行5−行4 是 algorithm 主张的全部分量**，审稿人最会盯。
  若行4、行5 拉不开，说明「双边」没超出「单边+分析拍平」，X 卖点落空——故**行4 是保护 X 不被证伪的控制，强烈建议跑**。
- **必跑骨架**：行 0/1/2/3/5。**行 4** 预算紧可晚跑，但答辩几乎必被问到。行 0 不自训，引已发表数字。
- **⚠️ 易埋的坑：行 2–5 必须同 compute、同任务集、同底座**，唯一变量是表里那一个。
  co-evolve 行天然比冻结行多花算力 → 不对齐 compute，行3−行2 会被「co-evolve 只是多训了」污染
  （这是审稿人质疑 self-evolving 类工作的标准弹药）。给冻结行补等量的额外 code-agent 训练步。

**最小可发表集 = 行 0/1/2/3/5 五个配置 + 二维难度热力图（§5）。**
脊证明「每个组件都在干活」，热力图证明「增益来自该来的地方（右下象限）」。

---

## 5. 核心分析框架：定位难度 × 修复难度 二维图

这是 Insight 层的载体，把抽象问题「是不是两个角色都得拉满」变成**可测量、可作图、可反驳**的命题。

- **横轴 = 定位难度**：这个 bug 找对地方有多难。
- **纵轴 = 修复难度**：找对地方之后，改对有多难。

每个任务是平面上一个点；把不同配置的通过率画成热力图，四象限直接回答问题：

- **左下（定位易、修复易）**：谁都能过，分析无功 → 零梯度区（即 §3.9 / §5 的有用带为空处）。
- **左上（定位易、修复难）**：瓶颈在 code agent，**分析再强也救不了** → 直接回答「不是两个都得拉满」。
- **右下（定位难、修复易）**：**分析角色的主场**，co-evolution 增益几乎全来自这里。
- **右上（两个都难）**：只有两个角色都强才过 → 唯一「必须都拉满」的象限。

**可证伪命题**：收益主要由**右下象限**驱动；只有右上象限要求双强。
若 benchmark 几乎没有右下象限的点，范式增量就很小。

**动态叙事**：随外循环 co-evolve，热力图「可过」前沿应从左下向右上推进；
若 X（双边信用分配）有效，前沿在**右下方向**的推进会显著快于轨迹级拍平 baseline——
那正是 analysis agent 被有效训练才能拿下的区域。

> 「有用带漂移」与「为什么这是最易忽略又最致命的坑」详见 §附录 A。

---

## 6. 待完成事项（按顺序执行）

### 6.1 【第一项 · 最高优先级】二维难度测量协议 —— 训练前的唯一闸门

> **零训练成本**（全是现成模型推理 + 数据集自带的 gold patch），却一次性锁定三件事：
> benchmark 选对没、分析侧尺寸假设成不成立、code 侧 7B vs 30B。这三件事若到训练阶段才暴露，代价极大。
> **在跑任何训练代码之前，先完成这一项。**

**两个轴各用一个 oracle 上界近似（注意：两个量是有顺序的条件量，从同一批 oracle rollout 出）：**

- **纵轴（修复难度）**：先固定 **oracle 定位**（用 gold patch 触及的文件/函数为中心，取 call graph 1–2 跳邻域
  + 相关 data flow 切片，作为「完美定位摘要」），看 `pass@k(oracle定位)` 本身多高。
  纵坐标取 `1 − pass@k(oracle)`（越高越难）。
- **横轴（定位难度）**：`pass@k(oracle定位) − pass@k(dump)`。差越大 → 找对地方越是瓶颈。

**最小可执行步骤：**
1. 取一个**现成、不训练的 7B code agent**（即 code 侧候选基座；**不要用 30B**，30B 会顶高通过率、压缩区分度）。
2. 对 benchmark 每个任务，跑三组、各 k 次采样（**k ≥ 5–10，三组同 k、同温度**）：
   - **dump 组**：喂无脑全量 AST/call graph（下界）。
   - **oracle-定位组**：喂 gold-patch 邻域切片（上界，定义「定位不再是瓶颈」）。
   - **空分析组（可选）**：纯 traceback、什么都不喂——分离「静态分析本身有没有用」与「切片有没有用」。
3. 每个任务落点：横 = `pass@k(oracle) − pass@k(dump)`，纵 = `1 − pass@k(oracle)`。画散点 / 热力图。
4. （code 侧选择验证）用 **30B** 重跑 oracle 组，看右下象限是否被大幅吃掉（变成左下）。吃得越多越印证「主实验用 7B/7B、30B 仅 scaling」。

**这张图回答的三个问题：**
- **A · benchmark 选对了吗**：看**右下象限**点占比。几乎空 → 天花板低，**换数据集，别开训**（最便宜的 idea 证伪）。
- **B · 分析侧该用小模型吗**：看**横轴整体分布**。横轴普遍高 → 定位普遍是瓶颈 → 分析活重 → 7B 小分析侧大概率不够，
  回到对称尺寸甚至加大；横轴普遍低、仅少数高 → 分析活相对轻，小模型站得住。**这张图直接判 §3.4 尺寸假设的死活。**
- **C · code 侧 7B vs 30B**：见步骤 4。

**两个必须防的坑：**
- ⚠️ **oracle 定位不能泄露修复内容**：用 gold patch 取**邻域**是模拟「完美定位」；
  **切勿把 patch 的 diff 文本塞进切片**，否则成了「给答案」，纵轴会被打穿、全测成 0。
  切片只含**位置 / 结构 / 数据流**，严格剔除 patch 的具体改动文本。**这是本协议最易翻车处。**
- ⚠️ **k 要够大且固定**：小 k 下 pass@k 方差大、象限归属会抖。三组同 k、同采样温度。

**本项产出**：一张（或随尺寸/配置分多张）二维热力图 + 三个问题的明确读数 + 一个 go/no-go 结论
（benchmark 是否可用、分析侧尺寸是否需调、code 侧定 7B 还是 30B）。

---

### 6.2 【第二项】冷启动方案落地（依赖 6.1 的尺寸结论）
- 大 code 模型冷启动：用 dump 当固定 baseline 分析，SFT / rejection sampling 暖到非平凡，确认「有分析 > 无分析」。
- 小 analysis 模型冷启动：以**固定的、已暖好的 code 模型**当评判者，用反事实信号（§3.6）从随机暖到非平凡。
  - 若 6.1-B 判定分析活重 / 或最终分析侧尺寸偏小（如 ≤2B 级），需**先加一道格式 SFT**确保结构化切片的格式遵循稳定，
    再进反事实暖启动。
- 闭环合上时机：两侧都暖到非平凡、且反事实通道有非零梯度后，才打开 co-evolve。

### 6.3 【第三项】信用分配机制 X 的具体形式（method 段的临门一脚）
把行 3（朴素拍平）与行 5（X）的机制写成可实现算法：
- 朴素拍平：terminal 如何摊给所有轮次与两个角色。
- X（双边多轮 credit）候选形式：逐轮反事实 / 过程奖励锚定 / 某种 advantage 分解——三选一或组合，需定清楚。
- 必须保证行 4（单边）与行 5（双边）只差「分析侧是否也拿正经 credit」这一个变量。

### 6.4 【第四项】交替冻结的训练调度
- 冻结—解冻的节奏（各训多少步 / 按什么指标切换）。
- 每次解冻 code agent 后，**有用带会漂移**（§附录 A）→ 需重新筛任务（课程更新），筛选指标复用反事实计算（组内方差近零的任务降权/丢弃）。

### 6.5 【第五项】多轮交互协议
- 回合预算上限；terminal 不奖励拖延的具体实现（防空转互捧，§3.3）。
- analysis agent 第二轮起如何条件于 code agent 的部分进展（部分 diff / 新报错）。

### 6.6 实验脊全表执行（§4）
按行 0/1/2/3/5（骨架）→ 行 4（控制）顺序跑，严格对齐 compute / 任务集 / 底座。

---

## 7. 风险登记表（提前知道哪里会出事）

| 风险 | 触发条件 | 缓解 |
|------|---------|------|
| benchmark 没有右下象限 | 6.1-A 显示右下点稀少 | 换数据集，别开训 |
| 分析活其实很重，7B 分析侧成瓶颈 | 6.1-B 横轴普遍高 | 回到对称尺寸或加大分析侧 |
| 系统冷启动饿死在原点 | analysis 随机、code 全 fail | §3.9 / §6.2 的 dump-baseline 暖启动 |
| 有用带随 code 变强漂移、梯度悄悄归零 | 训练曲线像收敛实则信息任务用尽 | §6.4 课程更新，复用反事实筛选 |
| 行5−行4 拉不开，X 卖点落空 | 双边 ≈ 单边+分析拍平 | 必须跑行 4 作控制；若真拉不开，重审 X 形式 |
| compute 未对齐污染 co-evolve 增益 | 冻结行比 co-evolve 行少训 | 给冻结行补等量 code-agent 训练步 |
| 两角色空转互捧不收敛 | 多轮、terminal 奖励了拖延 | 回合预算 + terminal 不奖励拖延 |
| 误用「特权信息」措辞被审稿人挑 | method/intro 出现 LUPI 字样 | 全文避开该词，用「精确符号信号替代 teacher」 |

---

## 附录 A：有用带（useful band）与漂移 —— 为什么它最易被忽略又最致命

analysis agent 的学习信号是反事实边际 `P(过|好分析) − P(过|dump)`。按「相对当前 code agent 的难度」分三段：
- **太简单**：code agent 看 dump 甚至不看分析都能改对，两概率≈1，边际≈0，无梯度。
- **太难**：给完美分析也改不动，两概率≈0，边际≈0，无梯度。
- **有用带**：好分析能把任务从 fail 顶成 pass，边际 > 0 —— **只有这段在教 analysis agent。**

若训练集大半落在前两段，分析通道**饿死**（大半 batch 零梯度，训练慢且抖，还可能误判成「分析没用」，其实是任务分布错）。
这与 GRPO 里「一组全对/全错则组内优势为零、白跑」是同一现象，只是发生在**任务层**而非样本层。

**漂移 + 与交替冻结的死锁**：有用带是**相对当前 code agent 能力**定义的。每次解冻、训强 code agent，
原本靠好分析才顶过去的任务可能变成「现在不看分析也能解」，滑出有用带，边界整体右移。
结果：固定训练集会**悄悄过期**，分析通道梯度不知不觉归零，曲线看着像收敛、实则信息任务用光。
这就是自博弈里永恒的移动靶（难度永远相对当前能力）。
它「最易忽略又最致命」正因为：开局看不出、发作时表现为平台期、修它要一整套「持续挖掘刚好在能力边缘的失败任务」的数据管线。

**安心点**：筛选指标和已在算的奖励是**同一套机器**——本来就要为每个任务跑几次 rollout 估 `P(过|baseline)` 与 `P(过|分析)`，
组内方差近零的任务直接降权/丢弃，边际高的留下甚至过采样。**课程筛选直接从反事实计算里掉出来，无需另搭。**

**最锋利的一句**：难度其实有**两个轴**——任务「可解不可解」与「瓶颈是不是定位」。
analysis agent 只从「定位是绑定约束」的任务学得到东西。
所以不是单纯按难度筛，是按「**定位是否为绑定约束**」筛（即 §5 的右下象限）。
若 benchmark 被「定位容易、修复烧脑」的任务主导，无论怎么调超参，analysis agent 都无事可学——
这会直接证伪整个 idea，却与算法对错无关，纯是数据集没选对。这正是 §6.1 必须最先做的根本原因。
